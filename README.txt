-A StopWatch Console App that would run for the number of seconds set in the Thread 
section of the code (1000 = 1 second as its in miliseconds).
-Would throw an exception if its already running and also if its not.
-Throwing Exceptions would prevent the app from breaking.